<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css" type="text/css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <title>WastEarn</title>

    <style>
        table{
            border-collapse: collapse;
            width: 100%;
            margin-top: 2%;
            margin-bottom: 5%;
            text-align: left;
        }
        th{
            padding-top: 1%;
            padding-bottom: 1%;
            color: white;
            background-color: #4CAF50;
        }
        td{
            padding-top: 1%;
            padding-bottom: 1%;
        }
        tr:nth-child(even){
            background-color: #f2f2f2;
        }
        tr:hover{
            background-color: #ddd;
        }
    </style>

</head>
<body>
    
    {{#if loggedin}}
      {{>userHeader}}
    {{else}}
      {{>header}}
    {{/if}}

    <div class="container">

        <h2 class="text-success my-3" style="text-align: center;">My Wastes</h2>

        <div class="container table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Type</th>
                        <th>Address</th>
                        <th>City</th>
                        <th>Status</th>
                    </tr>
                </thead>

                {{#each waste}}
                <tbody>
                    <tr class="orgList">
                        <td style="text-transform: capitalize;">{{this.type}}</td>
                        <td style="text-transform: capitalize;">{{this.address}}</td>
                        <td style="text-transform: capitalize;">{{this.city}}</td>
                        <td style="text-transform: capitalize;">{{this.status}}</td>
                    </tr>
                </tbody>
                {{/each}}

            </table>
        </div>

    </div>

    {{>footer}}

</body>
</html>